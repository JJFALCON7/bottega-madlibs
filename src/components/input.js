import React from 'react';

const Input  = (title) => {
    return (
        <div className="input">
            <input />
            <Label>{title}</Label>
        </div>
    )
}

export default Input;